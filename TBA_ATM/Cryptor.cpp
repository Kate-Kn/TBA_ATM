#include "Cryptor.h"

string Cryptor::doCryptPassword(const string& str) const {
   // printf("crypt(\"password\",\"ab\") = \"%s\"\n",crypt("password","ab"));
    //TODO add encryption
    return str;
}
